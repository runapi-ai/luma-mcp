{
  "mcpServers": {
    "luma": {
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/luma-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
